#Spring DataSources

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/web_shop?createDatabaseIfNotExist=true
spring.datasource.username=beotel
spring.datasource.password=Le@rn1ng
spring.datasource.initialization-mode=always
spring.datasource.platform=mysql

#Hibernate

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# SQL loggs

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE
spring.jpa.show-sql=true

#Server

server.error.whitelabel.enabled=false
server.error.include-stacktrace=always

#Thymeleaf

spring.thymeleaf.prefix=classpath:/templates

#Spring Security

spring.security.user.name=user
spring.security.user.password=user

# max file size & max request size- default 10MB

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB

# Global date and time formater iso

spring.mvc.format.date-time=yyyy-MM-dd HH:mm:ss
